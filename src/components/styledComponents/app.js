/* eslint-disable import/prefer-default-export */
import styled from 'styled-components';

export const StyledApp = styled.nav`
  [class*="col"] {
    padding: 0;
  }

  .container {
    padding: 0;
    .row {
      margin: 0;
    }
  }
`;
